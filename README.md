# aDock - Github Page

Before:

```
	$ gem install bundler
```

Then:

```
	$ git clone https://github.com/affear/adock.git
	$ cd adock
	$ bundle install
	$ jekyll serve --baseurl ''
```

Navigate to `http://localhost:4000`